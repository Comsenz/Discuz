name: Fix Code

on:
  push:
#  schedule:
#    - cron: '0 18 * * *'

jobs:
  fixcode:
    runs-on: self-hosted
    steps:
    - name: pull code
      run: |
        cd /home/wwwroot/discuss-github && git pull
      env:
       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    
