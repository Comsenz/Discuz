name: Fix Code

on:
  push:
#  schedule:
#    - cron: '0 18 * * *'

jobs:
  fixcode:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
      env:
       GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
